const url=$request.url const isLoadCart=url.includes('loadCart')const isParentalApproval=url.includes('RequestParentalApproval')try{if(isLoadCart){$persistentStore.write('','cartId')if($request.body){let body=JSON.parse($request.body)body.market="NG"body.locale="en-NG"body.friendlyName="cart-NG"$done({body:JSON.stringify(body)})}else{$done({})}}else if(isParentalApproval){let storedCartId=$persistentStore.read('cartId')if(storedCartId){if($request.body){let body=JSON.parse($request.body)body.cartId=storedCartId $notification.post('已发送尼日利亚家庭组购买请求！','Nigeria family group purchase request has been sent!')$done({body:JSON.stringify(body)})}else{$done({})}}else{$notification.post('警告，需要先添加购物车才能发起尼日利亚家庭组购买申请','Warning: You must add items to the cart first before initiating a Nigeria family group purchase request.')$done({})}}else{$done({})}}catch(error){$done({})}
